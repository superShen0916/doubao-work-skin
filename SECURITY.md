# 安全与隐私

## 本机调试接口

本工具以本地 CDP 控制已登录的桌面应用。启动时请求绑定 `127.0.0.1`；客户端拒绝 HTTP 重定向，以及指向远端或不同端口的 WebSocket 地址。每次 HTTP 请求与 WebSocket 建连前，通过 `lsof` / `ps` 检查监听进程的可执行文件是否为豆包工作主进程或独立浏览器；无法确认时拒绝连接。注入前还检查豆包工作聊天页的内置地址及布局锚点，跳过嵌入网页；恢复时只清理含本工具注入痕迹的页面。不要把调试端口映射到公网或局域网。

进程与页面校验限制的是本工具的连接目标，不会给调试接口增加访问认证。同一台电脑上的其他进程仍可能访问该接口。

`stop` / `restore` 和安装版 `disable` 不会关闭应用的 CDP 监听。需要关闭调试端口时，完全退出豆包工作，再正常启动应用。

清理运行中页面的外观需要 CDP 可连接。连接失败时，命令会报告恢复失败；安装版可用固定入口执行 `skin stop --keep-appearance`，源码版可用 `npm stop -- --keep-appearance` 停止 watch，再完全退出应用清除页面临时状态。删除状态文件不会终止仍在运行的 watch。

## 下载与主题资源

首次安装通过 HTTPS 从 `nodejs.org` 下载固定版本的 Node.js，并在解压和执行前核对安装脚本内固定的 SHA-256；缓存也会重新校验。程序与运行环境保存在用户目录，不修改豆包工作安装包，也不关闭系统安全保护。安装器本身应来自你信任的项目仓库或发布包。

主题包是可信本地输入，不是沙箱插件。加载器拒绝越出主题目录的资源路径和符号链接，但不会证明任意第三方 CSS 安全；CSS 中的远程资源也可能发起网络请求。内置背景使用本地资源并内联注入，无需从图片服务加载。

## 数据与诊断

常规换肤不主动读取消息正文、输入值、Cookie 或凭据，也没有遥测或上传逻辑。校验与诊断不主动输出页面 URL、窗口标题和按钮文字。可选 DOM 抓取工具保留结构信息（包括元素 ID、类名和有限属性），不能保证这些信息中绝无用户标识；输出文件仅供本地排查，分享前必须检查。

截图工具会生成当前页面的可见图像。临时隐藏侧栏和输入内容不能保证截图匿名；水印、对话或其他用户信息可能仍然可见，分享前需人工检查。

运行目录默认位于 `~/Library/Application Support/DoubaoWorkSkin/`。状态文件权限为 `0600`，目录权限为 `0700`。应用启动日志来自应用自身，可能含有敏感信息，不应原样上传。

## 报告安全问题

若发现漏洞，请优先通过托管平台提供的私密漏洞报告渠道联系维护者（如已启用）。若没有私密渠道，只提交不含利用细节与个人数据的联系请求，待维护者提供渠道后再分享复现材料。请勿在公开 issue 中粘贴 Cookie、凭据、对话内容或完整诊断快照。
