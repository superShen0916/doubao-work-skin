修复嵌套子菜单（例如推理强度选择）被裁剪，以及输入框顶部装饰层被误加背景、边框和阴影的问题，覆盖 13 套内置主题。

已有用户更新程序并重新应用皮肤后，也能获得这两处修复。加载器在内存中兼容旧主题规则，保留个人背景、配色、CSS 文件和上次选择；无需手动合并样式。

其他修正：注入器版本标记跟随包版本；端口回退不再尝试超过 65535 的端口。

发布流程现已自动化：推送与包版本一致的正式版本 tag 后，GitHub Actions 在 Node.js 22 / 24 上检查和测试，再打包、校验并上传。附件全部验证通过后才公开 Release。

下载与更新：

- `DoubaoWorkSkin-2.2.2-macos-scripts.zip`：完整解压后双击 `安装皮肤.command`。已安装用户也使用此入口更新，随后从桌面入口启动或让 Agent 重新应用并验证。
- `doubao-work-skin-2.2.2-skill.zip`：包含 Skill、安装程序及完整皮肤资源。
- 两个 `.zip.sha256` 文件：校验对应下载包。

本次界面修复已由维护者验收。自动验证覆盖全部主题及旧 CSS 的浏览器回归、临时目录内的升级与个人文件保留、失败回滚、ZIP 完整性及 SHA-256。开发验证环境为 macOS 26.5.1、Node.js 24 / 26；本机豆包工作版本为 2.29.12。自动化测试不代表额外完成了 Intel 硬件、Skill 导入或完整首次安装流程验收。

GitHub 发布不会自动更新用户电脑上的程序。请下载新版更新后重新应用皮肤；仅下载文件或保持旧版程序运行不会生效。
