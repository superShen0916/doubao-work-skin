修复 macOS「豆包换肤」App 在豆包工作已运行、普通启动返回一般错误时未尝试恢复的问题。此前仅错误码 `2` 会触发重试；现在任意非零错误码都会触发一次 `skin start --force`，由启动流程检查 CDP 并在需要时重启应用。

重试成功后激活豆包工作窗口；重试仍失败则显示错误提示并保留日志，不循环重试。应用未运行时仍直接启动，启动失败时报告错误。桌面 `.command` 入口和 Agent 的重启约束保持不变。

已有用户需下载新版完整安装包并重新运行 `安装皮肤.command`，以更新 App 内的启动脚本；仅更新源码或下载 ZIP 不会更新已安装的 App。安装保留已有个人皮肤和上次选择。打开 App 前请保存工作并等待当前 Agent 任务结束。

下载文件：

- `DoubaoWorkSkin-2.2.4-macos-scripts.zip`：macOS 安装包。
- `doubao-work-skin-2.2.4-skill.zip`：Skill 及完整安装资源。
- 两个同名 `.zip.sha256` 文件：对应下载包的校验和。

验证范围：在 macOS 26.5.1、Node.js 26.6.0 上，93 项自动化测试全部通过，源码与 13 套主题资源检查通过。新增隔离回归用例覆盖一般错误重试成功、重试失败后的退出码与提示，以及应用未运行时失败不强制重试。自动测试不启动或重启真实豆包工作，不替代本次故障场景的实机复现；Intel 硬件、无开发环境首次安装及 Skill 客户端导入仍需单独验证。

本版本仅包含 macOS 修复，不包含 Windows 分支改动。GitHub 发布不会自动更新本机安装。
