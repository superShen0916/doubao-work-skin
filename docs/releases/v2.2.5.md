改善聊天记录和输入框中链接、文档卡片的辨识度：增加半透明主题强调色背景，文档卡片内的链接不再重复叠加背景与间距。输入框链接不增加水平间距，避免影响编辑排版。

13 套内置主题的正文规则排除链接、按钮、SVG、chips 和资源标签，使用 `:where()` 避免额外提高选择器优先级。同步调整部分悬停文字颜色和用户消息的“展开全部”文字颜色。

开发检查新增聊天区域裸通配文字颜色检测，区分 `color` 与 `background-color`、`border-color`，忽略注释，并检查媒体查询及分组选择器。新增回归测试覆盖误报、漏检，以及各主题的链接背景、卡片嵌套、编辑器间距和键盘焦点。

已有用户下载新版完整安装包，重新运行 `安装皮肤.command`，再应用原主题并执行验证。共享链接背景随程序更新生效；安装保留已有主题文件、背景、配色及偏好，内置主题文件中的排除规则不会自动覆盖已有个人副本。无需为了获得共享背景样式覆盖个人主题；如需同步其他主题规则，应备份后按差异合并。GitHub 发布不会自动更新本机安装。

下载文件：

- `DoubaoWorkSkin-2.2.5-macos-scripts.zip`：macOS 安装包。
- `doubao-work-skin-2.2.5-skill.zip`：Skill 及完整安装资源。
- 两个同名 `.zip.sha256` 文件：对应下载包的校验和。

验证范围：在 macOS 26.5.1、Node.js 26.6.0 上执行源码与 13 套主题资源检查、96 项自动化测试，以及两种安装包的 SHA-256、CRC、文件清单、源码一致性和执行权限校验。Chrome 合成页面覆盖 13 套主题及旧主题规则，不连接或修改真实豆包工作。

使用者反馈当前实际使用正常，但本次未独立执行完整的真实客户端视觉验收，未记录新的豆包工作版本验收结果。嵌套加粗链接在合成页面中可能显示不同文字颜色，保留为非阻塞视觉差异；本版本通过背景区分链接。Intel 硬件、无开发环境首次安装及 Skill 客户端导入仍需单独验证。本版本不包含 Windows 分支改动。
